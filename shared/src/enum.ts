export const enum UserStatus {
	OFFLINE,
	ONLINE,
	LOBBY,
	PLAYING,
	SPECTATING
}

export const enum ChannelRole {
	USER,
	MODERATOR,
	ADMINISTRATOR
}

export const enum ChannelType {
	PUBLIC,
	PRIVATE,
	ANNOUNCEMENT,
	PERSONAL_MESSAGE
}
